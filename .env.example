# Environment Configuration Example
# Copy this to .env and fill in your actual values

# Google OAuth Configuration
REACT_APP_GOOGLE_CLIENT_ID=your-google-client-id-here

# Backend API Configuration
REACT_APP_AUTH_BACKEND_URL=http://localhost:3001/api/auth
REACT_APP_SERVER_URL=ws://localhost:8080

# WebRTC Configuration (Optional - fetched from backend in production)
REACT_APP_TURN_SERVER=turn:your-turn-server.com:3478
REACT_APP_TURN_USERNAME=your-turn-username
REACT_APP_TURN_CREDENTIAL=your-turn-password

# Security Settings
NODE_ENV=development

# Note: 
# - In production, set NODE_ENV=production
# - Demo login is automatically disabled in production
# - User authentication is validated via backend in production
# - ICE servers should be fetched from backend for security
